package com.onecric.CricketHDTV.presenter.live;

import com.onecric.CricketHDTV.presenter.BasePresenter;
import com.onecric.CricketHDTV.view.live.RankingView;


public class RankingPresenter extends BasePresenter<RankingView> {
    public RankingPresenter(RankingView view) {
        attachView(view);
    }
}
